source 'https://rubygems.org'

gemspec

platform :ruby_18 do
  group :test do
    gem 'rake'

    # For coveralls support
    gem 'mime-types', '~> 1.0'
    gem 'rest-client', '~> 1.6.7'

    gem 'test-unit', '~> 1.2.3'
  end
end
